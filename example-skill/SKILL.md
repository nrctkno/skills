---
name: example-skill
description: Template skill — copy this folder to start a new skill. Replace this description with a specific trigger condition (when should agent reach for this skill?).
---

Replace this section with step-by-step instructions for the task this skill covers.

- Keep it procedural: what to check, what order to do things in, what to avoid.
- Reference bundled files instead of inlining large content, e.g. see `references/details.md`.
- If the skill ships helper scripts, explain when to call them, e.g. `scripts/run.py`.
